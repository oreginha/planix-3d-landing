# VARIABLES DE ENTORNO PARA RAILWAY - BACKEND NODE.JS
# Copiar y pegar estas variables en Railway Dashboard

# === CONFIGURACIÓN DE PRODUCCIÓN ===
NODE_ENV=production
PORT=3001
TEST_MODE=false

# === SMTP CONFIGURATION (GMAIL) ===
# ⚠️ IMPORTANTE: Configurar estas variables con credenciales reales
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-app-password-gmail

# === EMAIL SETTINGS ===
EMAIL_FROM_NAME=Planix Web
EMAIL_FROM_EMAIL=noreply@planix.com.ar
EMAIL_TO=hola@planix.com.ar

# === CORS SETTINGS ===
ALLOWED_ORIGINS=https://planix-3d-landing-production.up.railway.app,https://planix.com.ar

# === RATE LIMITING ===
# Desactivado para evitar bloquear webhooks de Telegram
RATE_LIMIT_ENABLED=false
RATE_LIMIT_WINDOW_MINUTES=15
RATE_LIMIT_MAX_REQUESTS=100

# === LOGGING ===
DEBUG_LOGS=true
LOG_EMAILS=true

# === INSTRUCCIONES ===
# 1. Ir a Railway Dashboard > charismatic-fascination > planix-backend-node
# 2. Variables tab
# 3. Copiar cada variable (nombre=valor)
# 4. Especialmente importante configurar SMTP_USER y SMTP_PASS
